// Table constraints
pub const MAX_TABLE_NAME_LEN: usize = 64;

// Row layout
pub const NULL_BITMAP_SIZE: usize = 8;

// Metadata storage (temporary, should be stored differently in future)
pub const MAX_COLUMNS_STR_LEN: usize = 1024;
